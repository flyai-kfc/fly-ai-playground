# fly-ai-playground

FLY AI 팀의 **에이전트 개발 연습용 샌드박스**입니다. 실제 제품 코드가 아니라, 팀원들이
Git·에이전트 협업을 몸으로 익히기 위한 공간입니다. 마음껏 실험하고 깨뜨려도 됩니다.

## 이 레포에서 일할 때 지켜야 할 것

- **작업은 반드시 자기 폴더 안에서만.** `sandbox/<자기이름>/` 밖의 파일은 공동 작업 시간(Day 2)에만 건드립니다.
- **직접 main에 커밋하지 않습니다.** 항상 브랜치를 파고 PR로 올립니다.
- 브랜치 이름: `sandbox/<이름>-<주제>` (예: `sandbox/jimin-todo-cli`)
- 커밋 메시지: Conventional Commits — `feat:`, `fix:`, `docs:`, `chore:`
- **PR 본문에는 "무엇을/왜"를 반드시 씁니다.** 코드가 아니라 의도를 남기는 게 이 연습의 핵심입니다.
- 이 레포는 **public**입니다. API 키·토큰·비밀번호를 절대 커밋하지 마세요. 필요하면 `.env`를 쓰고 `.env.example`만 커밋합니다.

## 팀원을 도울 때

이 레포의 사용자는 대부분 개발이 처음입니다. 다음을 지켜 주세요.

- 명령어를 던지지 말고 **왜 그 명령을 쓰는지 한 줄 설명을 붙여** 주세요.
- Git 작업은 한 번에 한 단계씩 안내하고, 각 단계 후 결과를 확인하게 해 주세요.
- 에러가 나면 원인을 먼저 한국어로 풀어 설명한 뒤 해결책을 제시하세요.
- 사용자가 요구를 막연하게 말하면 바로 구현하지 말고, 먼저 무엇을 만들지 짧게 스펙으로 정리해 확인받으세요.
- 사용자가 이해하지 못한 채 넘어가려 하면 짚어 주세요. 이 레포의 목적은 결과물이 아니라 학습입니다.

## 각자 프로토타입 폴더 규칙

`sandbox/<이름>/` 안에는 최소한 다음을 둡니다.

- `SPEC.md` — 무엇을, 누구를 위해, 왜 만드는지 (구현 전에 먼저 작성)
- `README.md` — 실행 방법
- 실제 코드

## 진도 대시보드

`docs/index.html`이 팀 진도 대시보드입니다. 커밋·PR·리뷰 활동을 GitHub API로 읽어
자동으로 진척도를 계산합니다. 진도 데이터를 손으로 고칠 필요는 없습니다.
